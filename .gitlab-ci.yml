pages:
  stage: deploy
  script:
    - mkdir .public
    - cp -r ./* .public
    - rm -rf public
    - mv .public /
  artifacts:
    paths:
      - /
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH