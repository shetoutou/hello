pages: # 该任务命名必须是pages，是gitlab内部任务
  stage: deploy
  script:
    - mkdir .public
    - cp -r ./* .public
    - rm -rf public
    - mv .public public
  artifacts:
    paths:
      - static
  only:
   - master
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH