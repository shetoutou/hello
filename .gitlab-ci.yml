pages:
  stage: deploy
  script:
    - mkdir .public
    - cp -r ./* ./
    - rm -rf public
    - mv .public public
  artifacts:
    paths:
      - /
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH